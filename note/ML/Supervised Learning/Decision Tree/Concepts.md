Workflow
1. 特征选择
2. 决策树的生成
3. 决策树的剪枝：防止过拟合，简化模型，减少分支


#### 1. 特征选择
学习前，对特征进行选择，只留下对训练数据有足够分类能力的特征
学习时，递归地选择最优特征，并根据该特征对训练数据进行划分，使得对各个子数据集有一个最好的分类。

## 特征选择和决策树生成
通常特征选择的准则是信息增益或信息增益率
###  Entropy (信息熵)
信息熵是表示随机变量不确定性的度量。