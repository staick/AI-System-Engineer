## 表达式
$$
\begin{aligned}
f(x)&=\frac{1-e^{-2x}}{1+e^{-2x}}\\
f'(x)&=1-(\frac{1-e^{-2x}}{1+e^{-2x}})^2=1-f^2(x)
\end{aligned}
$$
### 求导过程

## 图像
![[Pasted image 20260825160931.png]]
## 概念
Tanh（双曲正切）将输入映射到区间 (-1, 1)。其关于原点中心对称。常用在隐藏层。
输入在\[-3, 3\]之外时，Tanh 的输出值变化很小，此时其导数接近 0。
Tanh 的输出以 0 为中心，且其梯度相较于 Sigmoid 更大，收敛速度相对更快。但同样也存在梯度消失现象。
## 代码实现
```python
def tanh(x):
	return (1 - np.exp(-2 * x)) / (1 + np.exp(-2 * x))
```
我们定义的这个函数在 x 为非常小的负数时可能会越界。实际上 Numpy 中已经实现了该函数，所以我们可以直接使用
```python
np.tanh(x)
```