神经网络也是机器学习的一种 
人工神经网络（Artificial Neural Network，ANN）简称 **神经网络（NN）**，是一种模仿生物神经网络结构和功能的计算模型。大多数情况下人工神经网络能在外界信息的基础上改变内部结构，是一种自适应系统(adaptive system)，通俗地讲就是具备学习功能。

使用多个[[Neuron|神经元]]就可以构建多层神经网络，最左边的一列[[Neuron|神经元]]都表示输入，称为 **输入层**；最右边一列表示网络的输出，称为 **输出层**；输入层与输出层之间的层统称为 **中间层（隐藏层）**。
![[Pasted image 20260827093357.png]]
相邻层的神经元相互连接（图中下一层每个神经元都与上一层所有神经元连接，称为 **全连接**），每个连接都会有一个 **权重**。
神经元中的信息逐层传递（一般称为 [[Forward Propagation|前向传播forward]]），上一层神经元的输出作为下一层神经元的输入。